import { HistoricoTransferencia } from './historico-transferencia';

describe('HistoricoTransferencia', () => {
  it('should create an instance', () => {
    expect(new HistoricoTransferencia()).toBeTruthy();
  });
});
